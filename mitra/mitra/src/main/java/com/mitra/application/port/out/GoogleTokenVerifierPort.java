package com.mitra.application.port.out;

import java.util.Optional;

public interface GoogleTokenVerifierPort {
    /**
     * Verifies the given Google ID token.
     * @param idToken the Token generated by Google on the client
     * @return the user's email if valid, otherwise empty
     */
    Optional<String> verifyToken(String idToken);
}
